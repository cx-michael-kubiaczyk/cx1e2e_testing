name: Nightly - main branches

on:
  schedule:
    - cron: '0 0 * * *'   # midnight UTC
  workflow_dispatch: {}    # allow manual trigger for debugging

jobs:
  run-suite:
    runs-on: ubuntu-latest
    env:
      CG_REPO: cxpsemea/cx1clientgo
      E2E_REPO: cxpsemea/cx1e2e
      CANARY_CX1_URL: ${{ secrets.CANARY_CX1_URL }}
      CANARY_IAM_URL: ${{ secrets.CANARY_IAM_URL }}
      CANARY_CLIENT_ID: ${{ secrets.CANARY_CLIENT_ID }}
      CANARY_CLIENT_SECRET: ${{ secrets.CANARY_CLIENT_SECRET }}
      CANARY_TENANT: ${{ secrets.CANARY_TENANT }}

      # Add PROD_* secrets here too if this job should also hit prod
    steps:
      - name: Checkout orchestrator repo (scripts)
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Checkout cx1clientgo (main)
        uses: actions/checkout@v4
        with:
          repository: ${{ env.CG_REPO }}
          ref: main
          path: cx1clientgo

      - name: Checkout cx1e2e (main)
        uses: actions/checkout@v4
        with:
          repository: ${{ env.E2E_REPO }}
          ref: main
          path: cx1e2e

      - name: Set up go.work linking both checkouts
        run: ../scripts/setup-workspace.sh cx1clientgo cx1e2e

      - name: Run full suite against Canary
        run: ../scripts/run-full-suite.sh
        env:
          RUN_LABEL: nightly-main

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nightly-main-results
          path: results/
          retention-days: 30
